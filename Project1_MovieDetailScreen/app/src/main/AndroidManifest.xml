<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.moviedetailscreen">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="영화 목록"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowAllCommentActivity" />
        <activity android:name=".CommentWriteActivity" />
        <activity android:name=".AvengersDetailScreen" />
        <activity android:name=".LittleForestDetailScreen" />
        <activity android:name=".HarryPorterDetailScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>